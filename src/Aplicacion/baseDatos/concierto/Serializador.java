package baseDatos.concierto;
/*
import java.io.FileWriter;
import java.io.IOException;
import java.util.ArrayList;

public class Serializador {
    public static void serializar(ArrayList<Evento> eventos) {
        try (FileWriter writer = new FileWriter("src\\Aplicacion\\baseDatos\\temp\\concierto.txt")) {
            for (Evento evento : eventos) {
                writer.write(evento.toString() + "\n");
            }
        } catch (IOException e) {
            System.err.println("Error al serializar los conciertos: " + e.getMessage());
        }
    }
}*/
